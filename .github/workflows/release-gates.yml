name: release-gates

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  npm-package:
    name: npm package (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm

      - run: npm ci

      - name: Unit tests
        run: npm run test:unit

      - name: Build dist (required for installer integration tests)
        run: npm run build:dist

      - name: Full test suite (unit + integration)
        run: npm test

      - name: E2E package test
        shell: bash
        run: npm run test:e2e

      - run: npm pack --dry-run

      - name: Pack package
        shell: bash
        run: |
          npm pack --json 2>/dev/null > npm-pack.json
          node scripts/pack-json.js npm-pack.json npm-pack-filename.txt

      - name: Install from packed tarball
        shell: bash
        run: |
          set -euo pipefail
          tmp="$(mktemp -d)"
          home="$tmp/home"
          mkdir -p "$home"
          tarball="$(cat npm-pack-filename.txt)"
          cp "$tarball" "$tmp/"
          cd "$tmp"
          HOME="$home" npx --package "./$tarball" citable install --agent claude-code --agent codex --agent cursor --skill '*' --copy --project --yes
          test -f .claude/skills/citable/SKILL.md
          test -f .agents/skills/citable/SKILL.md
          test -f .cursor/skills/citable/SKILL.md
          HOME="$home" npx --package "./$tarball" citable install --agent claude-code --agent codex --agent cursor --skill '*' --copy --project --yes
